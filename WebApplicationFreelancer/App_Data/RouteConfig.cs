using Microsoft.AspNetCore.Mvc;
using Microsoft.AspNetCore.Routing;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace WebApplicationFreelancer.App_Data
{
    public class RouteConfig : Controller
    {
       
        /*
         public static void RegistrerRoutes(RouteCollection routes)
         {
            // routes.IgnoreRoute("{ressource}.axd/{*pathInfo}");

             routes.MapRoute(
                 name: "Default",
                 url: "{controller}/{action}/{id}",
                 defaults: new { controller = "HomeController", action = "Index" } //, id = UrlParameter.Optional }
             );
         }
        */
        /*
         public IActionResult Index()
         {
             return View();
         }
         */
    }
}
